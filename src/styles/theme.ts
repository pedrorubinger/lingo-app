import { Colors } from "@styles/colors"
import { Fonts } from "@styles/fonts"
import { Spacing } from "@styles/spacing"

export const Theme = {
  name: "defaultTheme",
  colors: Colors,
  fonts: Fonts,
  spacing: Spacing,
}
